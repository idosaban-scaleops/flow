version: 2

project_name: flow

before:
  hooks:
    - go mod tidy
    # fang registers a hidden `man` command; generate the pages from it.
    - sh -c 'go run ./cmd/flow man > flow.1 2>/dev/null || true'
    # Completions are generated here, not by running the binary at install
    # time: the released binary is ad-hoc signed rather than notarized, so
    # Gatekeeper SIGKILLs it the first time Homebrew executes it.
    - mkdir -p completions
    - sh -c 'go run ./cmd/flow completion bash > completions/flow.bash'
    - sh -c 'go run ./cmd/flow completion zsh > completions/flow.zsh'
    - sh -c 'go run ./cmd/flow completion fish > completions/flow.fish'

builds:
  - id: flow
    main: ./cmd/flow
    binary: flow
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .FullCommit }}
      - -X main.date={{ .Date }}
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

universal_binaries:
  - id: flow
    replace: true
    name_template: flow

archives:
  - id: flow
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - README.md
      - LICENSE
      - src: flow.1
        dst: man/man1/flow.1
        info:
          mode: 0644
      - src: completions/flow.*
        dst: completions
        info:
          mode: 0644

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^test:'
      - '^chore:'
      - '^ci:'
      - Merge pull request
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: Fixes
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Documentation
      regexp: '^.*?docs(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: Other
      order: 99

homebrew_casks:
  - name: flow
    repository:
      owner: idosaban-scaleops
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: https://github.com/idosaban-scaleops/flow
    description: A personal workflow CLI for ticket-driven development
    binaries:
      - flow
    manpages:
      - man/man1/flow.1
    completions:
      bash: completions/flow.bash
      zsh: completions/flow.zsh
      fish: completions/flow.fish
    hooks:
      post:
        # The binary is not signed or notarized, so without this macOS
        # Gatekeeper refuses to run it ("flow is damaged and cannot be opened").
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/flow"]
          end

release:
  prerelease: auto
